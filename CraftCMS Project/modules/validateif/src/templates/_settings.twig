{# @var craft \craft\web\twig\variables\CraftVariable #}
{#
/**
 * Validateif plugin for Craft CMS
 *
 * ValidateifField Field Settings
 *
 * @author    Fruit Studios
 * @copyright Copyright (c) 2018 Refactory
 * @link      https://refactory.bg
 * @package   Validateif
 * @since     1.0.0
 */
#}

{% import "_includes/forms" as forms %}

{{ forms.selectField({
    label: 'Type'|t('validateif'),
    instructions: 'What would you like to validate.'|t('validateif'),
    id: 'type',
    name: 'type',
    value: field.type,
    options: field.types,
    errors: field.getErrors('type'),
    toggle: true
}) }}

<div id="custom" class="{{ field.type != 'custom' ? 'hidden' }}">

    {{ forms.textField({
        label: 'Regex'|t('validateif'),
        instructions: 'Validate this field with a custom regex rule.'|t('validateif'),
        id: 'regex',
        name: 'regex',
        value: field.regex,
        errors: field.getErrors('regex')
    }) }}

</div>

{{ forms.textField({
    label: 'Validation Message'|t('validateif'),
    instructions: 'Override the default validation message displayed on failed validation.'|t('validateif'),
    id: 'message',
    name: 'message',
    value: field.message,
    errors: field.getErrors('message')
}) }}

{{ forms.textField({
    label: 'Placeholder'|t('validateif'),
    instructions: 'Override the default input placeholder.'|t('validateif'),
    id: 'placeholder',
    name: 'placeholder',
    value: field.placeholder,
    errors: field.getErrors('placeholder')
}) }}



{#
{{ forms.lightswitchField({
    label: 'Toggle Visibility'|t('validateif'),
    instructions: 'Select this if you would like the toggle to show / hide the selected field'|t('validateif'),
    id: 'toggle',
    name: 'toggle',
    on: field.toggle,
    errors: field.getErrors('toggle')
}) }}

{{ forms.textField({
    label: 'Toggle Label'|t('validateif'),
    instructions: 'Label for the toggle field'|t('validateif'),
    id: 'label',
    name: 'label',
    value: field.label,
    errors: field.getErrors('label')
}) }}

{{ forms.selectField({
    label: 'Field'|t('validateif'),
    instructions: 'What type of field should we toggle?'|t('validateif'),
    id: 'type',
    name: 'type',
    value: field.type,
    options: field.fields,
    errors: field.getErrors('type')
}) }}
 #}

{#
{% for type in allFieldTypes %}
    {% set isCurrent = (type == className(field)) %}
    <div id="{{ type|id }}"{% if not isCurrent %} class="hidden"{% endif %}>
        {% namespace 'types['~type~']' %}
            {% set _field = isCurrent ? field : craft.app.fields.createField(type) %}
            {{ _field.getSettingsHtml()|raw }}
        {% endnamespace %}
    </div>
{% endfor %}
#}
